# 推理墙 UI 设计方案（维多利亚侦探主题）

## 气质与意象

**意象锚点：贝克街 221B 书房的案头**——深色胡桃木书桌上摊开的案情地图，黄铜放大镜压着一叠羊皮纸档案卡，软木板上用黄铜图钉钉着剪报与照片，旁边是墨水瓶和钢笔。推理墙不是"软件界面"，而是**侦探正在使用的一块实物案板**。

关键词：黄铜、羊皮纸、墨水、软木、档案、印刷品。

**校验第一直觉**：现有实现（黑底金边）已经是侦探游戏的"标配审美"，差异化在于把"贴图般的深色界面"变成"有物理质感的案板"——纸有纤维、铜有高光、墨有浓淡、卡有钉痕。而不是推翻现有配色重来（现有金/绿/蓝/红的功能色语义已被玩家熟悉，保留语义只提质感）。

## 配色系统（旧值 → 新值，保留功能色语义）

| 用途 | 现值 | 新值 | 意象来源 |
|---|---|---|---|
| 墙背景底 | (0.08,0.07,0.10) 冷黑 | `#191310` 深胡桃木 | 夜晚书房木案面 |
| 墙背景纹理 | 无（纯色） | 软木+织物纹理叠加 | 案板软木层 |
| 面板/顶栏/左栏 | (0.10,0.08,0.06) | `#221A11` → `#2A2117` 微渐变 | 木桌边缘 |
| 黄铜金（主强调） | (0.92,0.84,0.55) | `#E8D5A0`，高光 `#F4E8C0` | 磨亮的黄铜件 |
| 暗金（边框/分隔线） | (0.45,0.38,0.20) | `#6E5A32` | 氧化铜绿边 |
| 线索卡底 | (0.72,0.84,0.70) 冷浅绿 | `#C9D6B5` 羊皮纸绿 | 泛黄档案纸上的浅绿标签 |
| 假设卡底 | (0.72,0.80,0.92) 冷浅蓝 | `#BCC9DE` 纸蓝 | 蓝墨水钢笔稿纸 |
| 结论卡底 | 金系 | `#E9DDAC` 羊皮纸黄 | 结案报告用纸 |
| 人物卡底 | (0.78,0.72,0.55) | `#D9CDA9` 暖卡其 | 牛皮纸档案袋 |
| 墨水字（卡上深字） | (0.16,0.13,0.10) | `#2B2218` 棕黑墨水 | 铁胆墨水 |
| 干扰项红 | (0.95,0.30,0.30) 荧光感 | `#B03A2E` 印章红 | 火漆/印章红 |
| 暖白正文 | (0.92,0.88,0.80) | `#EAE0C8` | 旧纸墨字 |
| 次级文字 | (0.65,0.60,0.50) | `#A89878` | 磨淡的铅笔注记 |

## 墙背景（分层，从底到顶）

1. **底色层**：`#191310` 纯色（ColorRect，替换现值即可）。
2. **纹理层**：软木/织物无缝纹理 TextureRect（`modulate` 压暗至 8~12% 不透明度参与），盖在底色上，拉伸平铺。纹理用 AI 生成无缝图，约 1024×1024，随包入库 assets/ui/。
3. **暗角层**：四周径向暗角（用 4 个边缘渐变 ColorRect 或一张暗角纹理），中心亮、四角压暗，形成"台灯照亮案板中央"的灯光叙事。
4. **噪点层（可选）**：极淡的胶片颗粒纹理（3~5% 不透明度），消除大色块的塑料感。

## 文本框与面板样式规范

### A. 节点卡片（图谱内的线索/假设/结论/人物卡）
- **底**：对应羊皮纸色 + `shadow_size=6, shadow_color=#00000066, shadow_offset=(2,3)`（纸片浮在板上的投影）。
- **边框**：双线感——StyleBoxFlat 外框 1.5px 暗金 `#6E5A32`，卡片内部再留 3px 同色但 40% 透明度的内衬线（用内嵌 Panel 或 draw_rect 二次描边实现），模仿印刷品框线。
- **角**：圆角 3~4px（纸片不圆滑，保留硬朗）。
- **微旋转**：仅限**左栏静态列表卡**做 `rotation_degrees = ±1.2` 随机，制造手工钉上去的感觉；**图谱内交互卡不做旋转**（旋转会破坏拖拽命中坐标系，风险大于收益）。
- **未关联态**：边框虚线（现有机制保留），虚线颜色改暗金。
- **选中/悬停**：投影加深（shadow_size 6→10）、边框变亮金 `#F4E8C0`，不缩放（避免布局跳动）。
- **图钉**：卡片左上角画一个 4px 黄铜色圆点+高光点（draw_circle，纯代码），替代"图钉贴图"的复杂度。

### B. 弹窗（验证确认框 / 评价结果窗 / 假设选择窗）
- **遮罩**：`#000000` 75%，弹窗出现时遮罩 0.15s 淡入。
- **面板**：深棕 `#241C12`，圆角 10，外框 2px 黄铜金 `#E8D5A0`，**内衬 1px 暗金线**（间距 6px），双线框是维多利亚印刷品的核心识别。
- **标题条**：面板顶部一条 `#2E2417` 标题带，标题文字宋体加粗金色，标题带下缘 1px 暗金分隔线，两端各一枚小菱形装饰（◆ 用 Label/draw 即可）。
- **正文**：`#EAE0C8` 暖白，行距 1.4。
- **关闭 ✕**：右上角，悬停变印章红。

### C. 输入框（LineEdit：搜索框等）
- **常态**：底 `#14100B`（比面板更深一档，凹进去的感觉），边框 1px `#4A3C22`，圆角 4。
- **聚焦**：边框变黄铜金 `#E8D5A0`，底部 2px 亮金下划线（focus 样式），文字 `#EAE0C8`，光标金色。
- **占位符**：`#6B5F49` 灰金。

### D. 按钮（顶栏/弹窗/左栏）
- **常态**：底 `#2E2417`，边框 1px `#6E5A32`，圆角 5，文字 `#D8C89A`。
- **悬停**：底 `#3A2D1B`，边框亮金。
- **按下**：底 `#1E160D`，内容下移 1px（黄铜按钮按下手感）。
- **主行动按钮**（提交验证等）：底印章红 `#8C2F23` + 金边 1.5px + 文字 `#F4E8C0`（沿用现有红金语义，提纯色值）。
- **禁用**：全部降 40% 透明度。

### E. 顶栏 / 左栏面板
- 顶栏：`#221A11` 底 + 下缘 1.5px 暗金线（像书桌边缘的黄铜包边）。
- 左栏：底 `#20180F`，右缘 1px 暗金线；分组标题用小号宋体金色 + 前置装饰点。
- 标题字（"推理墙"等）：字号 26 改为宋体 SemiBold + 字间距 +2，颜色 `#E8D5A0`。

## 字体方案

- **现状**：全局 simhei.ttf（黑体），功能达标但与印刷品气质冲突。
- **建议**：标题/大字用 **Noto Serif SC（思源宋体）SemiBold**（书卷印刷感）；正文、小字、卡内文本维持黑体（低分辨率下宋体小字发虚，Godot Compatibility 渲染器 Web 端尤甚，混排是中文 UI 的成熟做法）。
- 引入方式：assets/fonts/ 新增 `NotoSerifSC-SemiBold.otf`（单字重，控制体积），仅用于 title_label 类节点；Godot 加载 FontFile 后 `add_theme_font_override("font", ...)`，不动全局主题（全局替换风险大）。
- 字号节奏：主标题 26 / 弹窗标题 20 / 卡片标题 15 / 正文 14 / 注记 12。

## 动效与交互

- 弹窗出现：0.15s 淡入 + 0.95→1.0 缩放（现有保留），关闭 0.1s 反向。
- 卡片悬停：投影加深过渡 0.12s，禁止位移/缩放（图谱坐标稳定优先）。
- toast：现有淡入淡出保留，底色统一深棕+金边。
- 图谱写入/连线：线宽 2px 暗金，关联完成后线色变亮金 + 0.2s 流光（可选，二期）。
- 禁止：弹跳、旋转入场等卡通动效——与维多利亚的克制气质冲突。

## 设计禁忌

- 不用纯黑 #000000、不用纯白 #FFFFFF（旧纸世界里没有绝对黑白）。
- 不引入蓝紫色渐变、玻璃拟态、霓虹发光——超出时代语境。
- 图谱内交互卡片不做旋转/缩放形变（拖拽命中系统依赖轴对齐）。
- 不加写实图钉/胶带贴图盖在交互区（遮挡点击热区）；装饰仅用代码绘制的低侵入元素。
- 功能色语义（绿=线索、蓝=假设、红=干扰）不推翻，只调质感。

## 实现落点（最小改动）

1. 色值集中替换：`reasoning_wall.gd` / `graph_view_controller.gd` 的 COL_* 常量区 + `wall_verify.gd` / `clue_observer.gd` 内联色值。
2. 新增 `scripts/clue/wall/wall_theme.gd`：统一提供 StyleBox 工厂（卡片/弹窗/按钮/输入框），逐步替换散落的 new StyleBoxFlat（可分期，先做弹窗与卡片）。
3. 背景分层：reasoning_wall.gd `_build` 里 bg ColorRect 之后插入纹理 TextureRect + 暗角层。
4. 纹理素材：AI 生成软木/织物无缝纹理 + 暗角图，入 `assets/ui/textures/`（小体积、jpg/webp）。
5. 字体：下载 Noto Serif SC SemiBold → `assets/fonts/`，标题节点替换。
6. 每步改完跑 `--headless -s` 编译冒烟 + 重新导出 Web 验证。

## 工作量分期建议

- **一期（纯色值+样式参数，无新资源）**：全局色板替换、弹窗双线框、按钮三态、输入框主题化、卡片投影——1 次会话可完成，效果立竿见影。
- **二期（新资源）**：背景纹理分层、暗角、宋体标题、图钉装饰点。
- **三期（增强，可选）**：连线流光、左栏卡片微旋转、颗粒层。
