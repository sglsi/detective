# 架构审计报告 — 维多利亚伦敦探案（detective）

> 审计时间：2026-07-26 ｜ 对照文档：`00_标准化开发目录 v2.17`、`E-25_技术架构专项文档`、`08_系统框架设计`、`项目进度评估与下一步计划（2026-07-17）`
> 实测方式：godot_project 文件系统扫描 + autoload 注册核对 + `Godot --headless --editor --quit` 全量编译（**0 错误**）+ tools/ 测试脚本与日志
> 设计准绳：结构化 / 模块化；用户硬性要求「一个项目只存在一套机制」

---

## 0. 审计结论（先看这里）

**架构骨架已经就位且健康**：7 条事件总线全部实现、统一场景基类 `DetectiveScene` 已让 scene1/2/3 复用、核心 autoload 单例分层清晰、全量编译零错误、后端确为 Supabase（与 E-25 一致）。

**但存在两类必须正视的问题：**

1. **「文档宣称完成」与「代码实际」严重不符**——进度文档称「8 幕场景 100% 实现」，实测仅有 **scene1/2/3 三幕可玩**，scene4–8 全无 `.gd/.tscn`（仅有对话 `.tres` 数据和引用它们的 stale 测试）。
2. **若干违反「单一机制」原则的多套实现**——主菜单有 3 个变体、线索数据存在「场景内联」与「`data/clues/*.tres` 资源」双源、且存在死链 `game_scene.tscn`。

下一步应**先收口可信度（统一机制 + 刷新测试 + 文档对齐），再补核心系统，最后扩场景 4–8**。

> **实现更新（2026-07-20）**：P3 scene4–8 已实现并接入 CI/流转链——5 幕全部落地为 `.tscn` + 继承 `DetectiveScene` 的 `.gd`，对话线索经 P3-0 新增的 `grants_clues` 走 `ClueSystem` 单一真相源，衔接链 `scene3→4→5→6→7→8→main_menu` 接通，新增回归单测 `test_dialogue_clue_grant.gd` 已接入 `run_ci.sh` §2.5。详见 §4 P3 #8 与 §2.C。

> **实现更新（2026-07-20 · P3.1）**：线索分级加权评分已落地（独立任务，≠ P3-1 死链修复）。`ClueSystem` 现按 `.tres` 的 `importance`（目录线索）或 `inline_weight`（内联线索）存储权重并派生 `tier`，`correct:false` 误导项恒 0 分；`StarRatingSystem.max_observation` 由 45 改为 **115**（= scene4–8 加权合计，见 P3.1 设计稿 §5）；对话/观察双路径透传权重、5 幕 `_award` 改 `total_weight` 累加；新增回归单测 `test_clue_tier_weight.gd` 已接入 `run_ci.sh` §2.5，`test_star_rating.gd` 满分断言动态化（与 115 基数解耦）。详见配套设计稿 `设计文档/P3.1_线索分级加权评分_改动设计.md`。

---

## 1. 当前已实现架构总览（实测）

| 层 | 已实现项 | 备注 |
|----|----------|------|
| 事件总线 | `Case/Scene/Dialogue/Clue/UI/Map/System` 共 7 条 | 与 E-25 §4.3 完全一致 ✅ |
| Autoload 单例 | `GameManager, CaseManager, DifficultyManager, ClueSystem, StarRatingSystem, SaveManager, SaveSystem, SettingsManager, AuthManager, APIManager, UIManager, ScreenManager, MapManager` 等 21 个 | 分层清晰 ✅ |
| 场景基类 | `DetectiveScene`（detective_scene.gd） | scene1/2/3 继承，本次刚统一推理墙/证据为单一机制 ✅ |
| 场景 | `main_menu` + `scene1/2/3/4/5/6/7/8` | 可玩路径 `scene1→scene2→scene3→scene4→scene5→scene6→scene7→scene8→main_menu`（scene3→scene4 衔接已于 P3 接通；scene8 完成回 main_menu 并 emit `case_completed` 触发结局） |
| 推理墙 | `reasoning_wall.gd`（唯一机制） | MVP 简化版（卡片+关联+四级判定），非 B-10 定义的三层五区+拖拽 |
| 对话 | `DialogueManager` + `DialogueRenderer` + `.tres` 资源（8 幕对话数据齐全） | 对话数据 ✅，仅 scene1/2/3 接入 |
| 后端 | Node + Supabase（Express + JWT + RLS） | 与 E-25 §6 一致 ✅ |
| 数据资源 | `data/clues/*.tres`（12 个）、`data/cases/*.tres`（1 个） | **存在但未被任何脚本 `load()`——孤儿资源** ⚠️ |

---

## 2. 三类清单

### A. 已开发 **并** 经过验证（Verified）

| 模块/架构 | 验证证据 |
|-----------|----------|
| 统一场景框架 `DetectiveScene` + 推理墙/证据单一机制 | `p15_architecture_unification` 通过；全量编译 0 错误 |
| `ClueSystem` + `ClueObserver` | `p5_5_clue_state_unit_test`（五态机单测）✅；`p14_save_load_clue_consistency` ✅ |
| `SaveManager` / `SaveSystem` 存读档 | `p10_e2e_scene_restore`、`p12_scene2_to_scene3_e2e`、`p14` ✅ |
| `DialogueManager` + `DialogueRenderer` | scene1/2/3 端到端集成通过（`p1`/`p11`/`p12`/`p13`）✅ |
| scene1/2/3 流程闭环 | 多轮 headless e2e ✅；0 SCRIPT ERROR |
| 7 条事件总线 | 全量编译扫描 ✅（autoload 注册完整） |
| 后端 REST API（Supabase） | `backend/tests` 20/20 通过（进度文档） |
| `AuthManager` / `APIManager` | 编译通过 + 云端存档链路（进度文档 P3 模块 8） |

### B. 已开发 **但** 未验证 / 验证薄弱（Developed, Unverified）

| 模块 | 状态说明 |
|------|----------|
| `StarRatingSystem`（三星评价） | 9星简化模型在用（obs上限115加权/推理14/洞察7，阈值0.3/0.6/0.9）；**P3.1.y 完善已落地**：新增 `get_evaluation_grade()`（GDD§2.6.5 名侦探/合格侦探/继续推理）、EASY 观察3星门槛降至0.8（§2.6.2）；修复 P3.1 引入的 `test_ending_system` 回归（45→max_observation、7星口径）。新增 `test_evaluation_grade.gd`（CI §2.5）。竞技倍率（`get_adjusted_total_stars`）由 P3.1.x 落地。剩余：徽章命名统一(08§3.10 BadgeID vs 运行时StarRatingSystem.Badge)、SPEED_RUNNER 无计时不放、`score_awarded` 信号未接(scene1-3不计分)。详见 `P3.1.y_评价体系完善_改动设计.md` |
| `DifficultyManager`（三难度） | 在用；**高优先三项已落地（P3.1.x）**：动态概率调整（基础0.7/上限0.95/停滞+10%/错误+5%/-5%）、困难隐藏提示选项（`filter_choices`+`is_hint_only`）、竞技分数倍率（0.5/1.0/1.5×，经 `StarRatingSystem.get_adjusted_total_stars` 生效）。新增 `test_difficulty_enhanced.gd`（CI §2.5）。剩余未做：误导概率/差异化提示文本/推理墙自动填充/检验次数/验证报告差异化/线索失效/凶手行动线/可信度展示（见 `设计文档/验证_三难度模式运行机制.md` 与 `P3.1.x_三难度高优先项_改动设计.md`） |
| `CaseManager` | 无单测 |
| `UIManager` / `ScreenManager` | 仅集成验证 |
| `MapManager`（地点/地图面板） | 部分实现，无单测 |
| `AudioManager` | 模块在，但 options 面板标注「音效与音乐 — 即将开放」→ **未接线** ⚠️ |
| `tool_bar`（侦破工具） | **仅放大镜**，其余 7 工具 + 组合发现机制未接 |
| `reasoning_wall.gd` 判定逻辑 | 集成在用，但**无专门 verdict 单测** |
| `data/clues/*.tres`、`data/cases/*.tres` | `data/clues/*.tres`：**P1-3 已接入** 16 条观察线索（`collect_clue_from_catalog` 取目录权威 name/desc，内联回退）；案件证据类 .tres 与 `data/cases/*.tres` 仍仅解析未接 UI（独立事项） |
| 主菜单 ×3（`main_menu` / `_v2` / `_mini`） | 多套并存，违反单一机制（风险 1） |
| `game_scene.tscn` | 被 `difficulty_select.gd:46` 引用但**文件缺失**（死链，风险 2） |
| 测试套件本身 | `p2_runtime_check`/`p2_classname_check`/`p4_walkthrough` 引用不存在的 `scene_04..08` → **现会失败/误导**（风险 4） |

### C. 按文档 **应存在但代码中缺失**（Not Developed）

**场景内容（最大缺口）**
- **scene4–8（血字的研究 后 5 幕）**：**✅ 已于 P3 实现**——5 幕全部落地为 `.tscn` + 继承 `DetectiveScene` 的 `.gd`；对话线索经 `grants_clues` 走 `ClueSystem` 单一真相源（含 403/603 等误导项 `correct:false`）；衔接链 `scene3→4→…→8→main_menu` 已接通（见 §4 P3 #8）。

**核心系统 / 子系统（E-25 §4.2 的 24 子系统里缺失项）**
- `BadgeSystem`（徽章 铜/银/金，B-11 要求）— 缺
- `ProgressSystem` / `TimelineSystem`（调查进度系统 + B-5 叙事时间线）— 无独立模块（`game_state_machine` 部分替代）
- `KnowledgeBaseSystem`（推理知识库，B-10 / 04 文档）— 缺（六步闭环「知识检索」步无系统支撑）
- `EndingSystem`（结局/通关系统）— 缺
- `ThemeManager` — 缺（以 `ui_theme.tres` 代替）
- `AnimationController` — 缺（动画内联）
- `SceneTransition` 全局节点 — 缺（`game_state_machine` 代）
- `SyncManager`（云同步编排）— 缺（SaveManager 直调 API）
- `AdManager` — 缺（未来）
- `AnalyticsManager`（E-27 埋点）— 缺
- `LocalizationManager` / `FontManager` / `ResourceManager`（I18n + Data 模块）— 缺

**专项系统完整度**
- 侦破工具系统（D-22.1.11）：仅放大镜，缺其余 7 工具 + 工具×物品组合发现
- 地点系统（B-6）：`MapManager` 部分，缺「地点三态 / 案件驱动式加载」
- 角色系统（B-7）：仅 `holmes_pixel.gd`，缺 `RoleSystem` / 知识边界
- 干扰与隐藏机制（C-18 五类误导）：场景内硬编码干扰项，无系统

---

## 3. 关键架构风险（违反结构化/模块化原则）

1. **多套主菜单**（已收敛，P1-1）：`main_menu.tscn` 现挂唯一脚本 `scripts/ui/main_menu.gd`；`main_menu_v2.gd/.tscn`、`main_menu_mini.gd` 已加 DEPRECATED 标记并于 2026-07-26 待本地删除（删除被沙箱阻断，需在本机执行；全局已确认无悬空引用）。
2. **死链（已基本消解）**：`difficulty_select.gd:46` 经 P3-1 复查已指向 `res://scenes/scene1.tscn`（原「跳转 game_scene.tscn 不存在」未复现，可能早已在 P1-2 修复）；`scene3.gd` 末尾 `FIXME: change to scene4` 已改为 `scene4.tscn` 并于 P3-7 接通 scene3→scene4 流转。`framework_test.gd` 现已断言 scene2–8 存在（本次补齐），不再依赖 game_scene。
3. **线索数据双源**（已缓解，P1-3）：MVP 场景原用内联热点字典、`data/clues/*.tres` 为影子源未接入 → 已实现 `ClueSystem.collect_clue_from_catalog` 单点收口：收集时优先取 `.tres` 目录权威 name/desc，缺失回退内联，`correct` 不覆盖。**已知残留**：运行时短 id（wrist/c201）与文档 `C_SOTCB_xxx` 未对齐、案件证据 .tres 未接 UI——属 P3 内容迁移，非 P1 阻断项。
4. **测试套件 stale**：`p2_*`/`p4_walkthrough` 引用不存在的 `scene_04..08`，使 CI/自测结果失真，掩盖真实覆盖率。
5. **文档与代码不符**：进度文档（2026-07-17）称「8 幕 100%」，实测 3 幕 → 需纠偏，否则误导后续规划。
6. **推理墙范围**：B-10 定义「三层架构+五区布局+拖拽」，当前 `reasoning_wall.gd` 为简化版 → 需确认是否满足 MVP 范围（属 0.3.2 待验证假设 #1）。

---

## 4. 下一步开发计划（按优先级）

### P1 — 收口可信度（🔴 高，紧扣「避免多套机制」）
1. **收敛主菜单**：保留一个（建议 `main_menu`），将 `_v2`/`_mini` 标记为废弃或删除；统一入口。
2. **修死链**：`difficulty_select.gd` 改为跳转真实场景（如 `scene1` 或新增 `game_scene`），并同步修 `framework_test.gd` 断言。
3. **数据驱动统一（✅ 2026-07-26 已完成收口）**：scene1–3 的线索收集改走 `ClueSystem.collect_clue_from_catalog`（目录优先、内联回退、`correct` 不覆盖），并为 16 条可玩线索补 `clue_*.tres`。**未做**：运行时短 id 重命名为文档 `C_SOTCB_xxx`、案件证据 .tres 接 UI——属 P3 内容迁移，盲改会牵动存读档/推理墙/测试，故缓。
4. **刷新测试套件**：删除/修复引用 `scene_04..08` 的 stale 测试；补 `reasoning_wall` 判定单测、`star_rating`/`difficulty`/`case_manager` 单测；确保 `run_ci.sh` 绿。
5. **对齐文档**：将进度文档的「8 幕 100%」修正为「3 幕可玩 + 5 幕仅数据」，并标注本审计报告为最新 reality。

> **线索机制设计 vs 实现差距（②）收口结论（2026-07-27）**：设计侧「关键/重要/一般/其他/误导」五分类此前在运行时塌成 `correct` 布尔；**P3.1（2026-07-20）已收口为加权评分**——`ClueSystem.collect_clue` 增 `weight` 参数并派生 `tier`、`StarRatingSystem` 满分基数 45→115 与权重解耦、`test_clue_tier_weight` 接入 `run_ci.sh`。剩余设计项（3 类标签体系、可信度 5 档、支持度完整公式、时效/难度过滤）在 `06_推理墙运行机制.md` / `00_核心设计思路.md` 中**明确标为 M2+/M3 延期**，属计划内范围裁剪，**非一致性缺口**。遵循「单一机制」原则，不在本项目新造平行标签/可信度系统；若日后需要，仅在 `ClueSystem.collected_clues` 字典上增字段扩展，不另起漏斗。

### P2 — 补齐核心系统（文档要求但缺失）
6. `BadgeSystem`（接 `StarRatingSystem`）—— **✅ 2026-07-26 已完成**：新建 autoload `badge_system.gd`，监听 `SystemEventBus.case_completed` 消费 `StarRatingSystem.evaluate_badges()` 评定结果，跨案累积解锁集合、经 `SaveManager` 存档（`badges` 键）、广播 `badges_updated`，含单测 `test_badge_system.gd` 接入 `run_ci.sh`。**偏差**：运行时采用 `StarRatingSystem.Badge` 枚举（KEEN_EYE 等 7 枚，已验证），与 08 文档 B-11.2 另列 7 枚命名（初出茅庐/神枪手…）不一致，未另起一套以免双源，待统一。
   `ProgressSystem`/`TimelineSystem`（调查进度+叙事时间线，事件驱动）、`KnowledgeBaseSystem`（推理知识库 7 大域 + 关键词检索 + 笔记）、`EndingSystem`（四档结局，按总星级占比）—— **✅ 2026-07-26 已完成**：均新建 autoload 并经 `SaveManager` 持久化、广播各自事件总线信号，各带单测接入 `run_ci.sh`。`ProgressSystem` 按 09 审核报告 P0-5 将 `TimelineSystem` 收敛为「调查进度系统（事件链驱动）+ 叙事时间线（纯氛围层）」。
7. 侦破工具系统补全（8 工具 + 组合发现），落实 D-22.1.11 —— **✅ 2026-07-26 已完成**：新建 autoload `tool_system.gd`，8 工具定义 + 解锁管理 + 工具×物品组合发现（seed 规则，可由案件 .tres 扩充），复用 `SceneEventBus.tool_used`（顺手修正其信号为 2 参数，与 `tool_bar.gd` 发射一致）驱动发现，避免平行实现；含单测 `test_tool_system.gd` 接入 `run_ci.sh`。

### P3 — 内容广度（MVP 8 幕）
8. **实现 scene4–8**：**✅ 2026-07-20 已完成**——复用 `DetectiveScene` 基类（继承 + 内容钩子），5 幕（scene4–8）全部落地 `.tscn`+`.gd`；对话线索经 P3-0 新增的 `grants_clues` 走 `ClueSystem.collect_clue_from_catalog` 单一真相源（含 403/603 等误导项 `correct:false`）；衔接链 `scene3→4→5→6→7→8→main_menu` 接通；scene8 完成 emit `case_completed` 触发 EndingSystem 评定。新增回归单测 `test_dialogue_clue_grant.gd`（验证对话线索真收集 + 幂等）已接入 `run_ci.sh` §2.5；`framework_test.gd` 已补 scene2–8 存在断言。美术缺口（sc_04–08 背景 / sherlock 头像）仍 SKIP，不阻塞可玩性（见 P3 设计稿 §7）。
9. 地点系统三态 / 角色系统知识边界。

### P4 — M2+ 扩展（文档标注待验证/未来）
10. `SyncManager` / `AnalyticsManager`(E-27) / `AdManager` / I18n(`Font`/`Localization`) / `ThemeManager` / `AnimationController` / `SceneTransition`。

---

## 5. 一句话路线

> **骨架已立、编译已绿、部分已验证；但「文档说完成」≠「代码真完成」。下一步不是急着加功能，而是先收口可信度（统一机制 + 刷新测试 + 对齐文档），再补缺失核心系统，最后把 scene4–8 从「仅有对话数据」变成「可玩场景」。**
