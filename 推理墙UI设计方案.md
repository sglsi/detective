# 推理墙 UI 设计方案（v3）

> 本方案定义推理墙（图谱 + 弹窗 + 顶栏 + 左栏）的视觉升级规范。
> v3 变更：新增「顶栏功能按钮融合设计」（将现有全部功能按钮纳入新 UI）；界面主标题定名「推理战场」。
> 实施原则：不改变任何功能与交互机制，只升级视觉表达；功能色语义保留。

---

## 一、设计理念

把推理墙从"深色软件界面"变成**贝克街 221B 书房里侦探正在使用的实物案板**。
意象锚点：黄铜工具、羊皮纸、墨水、暗色胡桃木案面，以及织进背景的**伦敦旧地图线稿**——
玩家不是在"操作软件"，而是伏在案前的伦敦地图上推演案情。

从参考图（《红发会案件》关系图谱界面）吸收的思想与取舍见第九节。

## 二、命名

- 界面主标题（牌匾）：**推理战场**
- 牌匾副行：`案件一 · 血字的研究 · 1881`（随案件数据变化）
- 说明：模块内部代码命名、文档指代仍可用"推理墙"，**仅界面展示文案用"推理战场"**。

## 三、配色方案（色值对照表）

| 用途 | 色值 | 意象来源 |
|---|---|---|
| 墙底色 | `#191310` | 深胡桃木案面 |
| 顶栏/面板底 | `#221A11` | 书架背板深木 |
| 弹窗面板 | `#241C12` | 案头文件盒 |
| 主金色（边框/强调） | `#C9A961` | 黄铜件 |
| 暗金（次级边框/线） | `#8A7040` | 氧化黄铜 |
| 羊皮纸-线索卡 | `#B8C4A8`（淡鼠尾草绿） | 绿=线索 语义保留 |
| 羊皮纸-假设卡 | `#A8B8C4`（灰蓝） | 蓝=假设 语义保留 |
| 羊皮纸-结论卡 | `#C4B890`（陈年米黄） | 黄=结论 语义保留 |
| 羊皮纸-人物卡 | `#B8A888`（卡其） | 暖牛皮纸 |
| 干扰/矛盾强调 | `#B86A4A`（赭红） | 封蜡/印章红（禁用亮红） |
| 卡内正文字 | `#3A3226` | 墨水黑棕 |
| 界面正文字 | `#D8CBB0` | 羊皮纸亮米色 |
| 输入框底 | `#14100B` | 比面板更深一档（凹陷） |

## 四、墙背景（四层叠加）

1. **底色**：`#191310`（替换现有冷黑 `0.08,0.07,0.10`，改一个常量）。
2. **地图暗纹**：1864 伦敦街道线稿纹理（TextureRect 平铺），压暗至 5~8% 参与感，棕褐色调——"在地图上推理"。
3. **暗角**：四周压暗、中央亮（台灯照亮案板叙事）。
4. **颗粒**（可选三期）：极淡胶片噪点，消除大色块塑料感。

## 五、文本框样式（五类）

### A. 节点卡片（线索/假设/结论/人物）——v5 起**保持现有形态，材质升级**

**v5 澄清（对齐实际）**：现有推理墙本来就是横向树（人物最左 → 线索 → 推断 → 结论向右展开，见实际截图），**布局逻辑与卡片形态完全保持现状**——小尺寸矩形卡、文字居中、各列不同色。本方案不改结构、不改尺寸、不改交互，**只把卡片背景从纯色 StyleBoxFlat 升级为羊皮纸材质贴图**，并叠加双线金边/投影。

- **卡片形状**：维持现状（小矩形、居中文字、结论列横条卡），不引入大横卡。
- **内部布局（左右分区）**：
  - 文字：保持现状（居中，尺寸/字号不动）；人物卡沿用窄卡，圆形肖像徽章作为**人物卡左缘内嵌元素**（40px 圆，暗金描边，复用 portraits），不改变卡片外形。
- **材质背景（本方案核心，替代纯色）**：以 `assets/ui_preview/材质_羊皮纸卡片.png` 裁出四色卡纸，对齐**实际游戏列语义**——**人物=红棕羊皮纸 / 线索=棕褐羊皮纸 / 推断=蓝灰羊皮纸 / 结论=浅绿羊皮纸** → **StyleBoxTexture 九宫格**（margin 取 deckled 纸边带，中心拉伸、四角保真）→ 四类卡片分别套用对应色纸贴图；文字色相应调为各纸色上的深色墨字（暗棕/藏蓝/暗绿），保证纸上可读。金边双线与图钉点仍由代码叠加绘制（贴图负责纸纹，代码负责框线与投影）。
- 边框：**双线**——外 1.5px 暗金 + 内衬 3px 半透明细线；圆角 3~4px。
- 投影：shadow_size 6，offset (2,3)，黑 40%。
- **图钉点**：左上角代码绘制黄铜圆点（直径 6px，含高光）。
- 悬停：仅投影加深（shadow_size 6→9），**不做旋转**——图谱交互卡旋转会破坏命中坐标系（本项目已踩坑）；±1.2° 微倾仅用于左栏静态列表。

### A2. 布局（v5 澄清：保持现状，不改造）

- **现有布局保持不动**：横向树（人物最左 → 线索 → 推断 → 结论向右展开）、星型树坐标计算、钉位/拖拽/rebuild/折叠/聚焦全部维持现状——`_compute_layout` 等布局代码**零改动**。
- **16:9 基准**：设计基准 1920×1080（预览画布 16:9）。现有布局在 16:9 下正常展开，fit_view 已有缩放兜底。
- **连线**：细暗金线（现有连线仅换色加质感）；悬停/选中边显示「支撑/指认」胶囊标签（线中点），平时隐藏。
- 左栏 346px 与图谱让出区契约（Q6）全部不动。

### B. 弹窗（验证确认框 / 评价窗 / 教程窗）
- 深棕面板 + **双线框**：2px 黄铜外框 + 内衬 1px 暗金线（间距 6px）——维多利亚印刷品核心识别。
- 标题：金色宋体 + 下方细分隔线；**评价窗特有**：「推理结论」标题用印章红宋体，正文下方加手写花体 `Sherlock Holmes` 签名行（花体字体或预渲染贴图）。
- 圆角 8px；遮罩 0.15s 淡入。

### C. 输入框 / 下拉选择框
- 底 `#14100B`（凹陷感），聚焦时边框变亮金 + 底部 2px 金色下划线。
- 下拉框（焦点人物/过滤）：同款凹陷框，右端小金三角指示。

### D. 按钮（统一三态）
- 常态：深棕底 + 1px 暗金边；悬停：金边亮起（`#C9A961`）；按下：底色加深 + 内容下沉 1px。
- **主行动「提交验证」**：印章红底（`#B86A4A`）+ 金边——全界面唯一红色按钮，视觉锚点。
- 次级钮前缀小图标 + 短文字（图标不替代文字，保证中文玩家可读性）。

### E. 顶栏 / 左栏容器
- 顶栏：下缘 1.5px 暗金线（书桌黄铜包边）；左栏：右缘同款细线。
- 分组标题：金色小宋体。

## 六、顶栏功能按钮融合设计（v3 新增）

现有顶栏两行按钮全部纳入新样式，布局保持现有功能分区不变：

### Row 1（主行）
```
[牌匾: 推理战场 — 血字的研究] [难度徽章] | 画笔工具组 | ……弹簧…… | [模式] [焦点人物▾] [↶][↷] [提交验证] [求助] [适应] [自动排列]
```

| 控件 | 新样式 |
|---|---|
| **牌匾标题** | 羊皮纸卷边牌匾（deckled 边、两端小卷角），金色宋体「推理战场」，副行小字案件名+年代；替代现有纯文字标题 |
| **难度徽章** | 小铜牌：简单=银灰 / 普通=黄铜 / 困难=红铜，1px 暗金描边圆角 2px |
| **线型组（实线/虚线）** | "墨水笔尖"圆形黄铜钮（直径 36px）：钮心为笔尖图标，激活态=压陷 + 亮金描边；组标签「线型」金色小宋体 |
| **性质组（支持/矛盾存疑/反对/弱关联）** | 同款圆钮，**钮心用语义色点**（绿 `#7A9A6A` / 橙 `#C4955A` / 赭红 `#B86A4A` / 灰 `#8A8578`）标示边性质，悬停 tooltip 显示全名；组标签「性质」 |
| 组间分隔 | 2px 黄铜竖隔条（圆头），区分画笔组/视图组 |
| **模式（人物星型）** | 铜牌文字钮：星芒小图标 +「人物星型」 |
| **焦点人物下拉** | 羊皮纸凹陷选择框（C 类样式），宽随最长人名 |
| **撤销/重做 ↶↷** | 弯箭头圆形黄铜钮；禁用态钮心变暗 50%、不可点 |
| **提交验证** | 印章红主行动钮（D 类），唯一红色锚点 |
| **求助 / 适应 / 自动排列** | 铜牌文字钮：救生圈 / 指南针 / 齿轮 小图标 + 短文字 |

### Row 2（次级行）
```
[🔍 搜索线索/推断/人物...] [过滤▾] …… [折叠] [导出] [存档导出]
```

| 控件 | 新样式 |
|---|---|
| **搜索框** | C 类凹陷输入框 + 放大镜金色图标 + 金色下划线（聚焦时） |
| **过滤下拉** | C 类凹陷选择框 |
| **折叠 / 导出 / 存档导出** | 铜牌图标钮（折扇 / 封蜡章 / 钥匙 图标 + 短文字），排在 Row2 右端次级组 |

**统一节奏**：钮高 36px；圆钮直径 36px；铜牌钮圆角 2px、圆钮圆角 18px；组内间距 8px、组间 16px。
**交互不变**：所有按钮的回调、快捷键、禁用逻辑零改动，仅换视觉皮肤。

## 七、字体

- 标题类（牌匾/弹窗标题/分组标签）：**思源宋体 SemiBold**——需子集化控制在 ~1MB 内（Web 端加载约束）。
- 正文与卡内小字：**保持黑体**（宋体小字低分辨率发虚；混排是中文 UI 成熟做法）。
- 评价窗签名：花体英文字体或预渲染贴图 `Sherlock Holmes`。

## 八、动效（克制）

- 弹窗：0.15s 淡入 + 0.95→1.0 缩放。
- 卡片悬停：仅投影过渡。
- 按钮：三态瞬切（不弹跳）。
- **禁止**：弹跳/旋转入场、流光、霓虹等卡通/科技动效。

## 九、参考图取舍（吸收与不照搬）

**吸收**：①伦敦地图暗纹画布 ②人物卡圆形肖像徽章 ③边关系标签（**仅悬停/选中边时**显示「支撑/指认」小胶囊，常驻会太吵） ④顶栏案件牌匾 ⑤评价窗钢笔签名 ⑥可折叠图例（底部「?」钮，展开显示节点颜色图例） ⑦左栏进度徽章「线索 5/8」。

**不照搬**：①亮牛皮纸画布（节点密度高、拖拽交互，亮底淹没对比） ②右侧常驻大面板（挤压图谱交互区，功能与左栏+弹窗重复） ③左侧圆形导航栏（左栏是工作区不是导航）。

## 十、实施分期

- **一期（无新资源，纯样式+布局参数）**：全局色板替换、弹窗双线框、按钮三态（含印章红主行动钮）、输入框主题化、卡片投影+图钉点、牌匾标题结构与「推理战场」文案、**横排推理链布局改造（`_compute_layout` 列向计算）+ 横排卡内部布局重排**。
- **二期（需新资源）**：羊皮纸卡纸贴图裁切与 StyleBoxTexture 接入（材质_羊皮纸卡片.png → 四色九宫格）、地图暗纹纹理、人物肖像徽章、宋体标题（子集化）、签名花体、左栏微倾。
- **三期（可选）**：边关系标签、左栏进度徽章、图例条、颗粒层。

每期完成后：编译冒烟 → Web 导出 → 打版本戳 → 预览验证。

## 版本记录

- v4（本次）：推理链改横排（列流向 左→右）、卡片横排化（240×88，徽标区+文字区）、羊皮纸真实材质贴图方案（材质_羊皮纸卡片.png 九宫格）、16:9 设计基准（1920×1080）。
- v3：顶栏 13 个功能按钮全部纳入融合设计（三区布局、画笔组黄铜圆钮、印章红主行动钮）；标题更名「推理战场」。
- v2：融合参考图（地图暗纹画布、肖像徽章、悬停边标签、牌匾、钢笔签名、可折叠图例）；明确三处不照搬。
- v1：深色软木案面基线方案（色板、五类文本框样式、动效禁忌）。
